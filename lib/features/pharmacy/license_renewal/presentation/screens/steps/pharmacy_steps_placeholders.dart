import 'package:flutter/material.dart';

class Step4PharmacyReminder extends StatelessWidget {
  const Step4PharmacyReminder({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Reminder Setup Placeholder"));
}

class Step5PharmacyReview extends StatelessWidget {
  const Step5PharmacyReview({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Review Placeholder"));
}

class Step6PharmacySuccess extends StatelessWidget {
  const Step6PharmacySuccess({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Success Placeholder"));
}

class PharmacyStatusScreen extends StatelessWidget {
  const PharmacyStatusScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text("Status Tracking Placeholder")));
}
